import React from 'react';
import {Box, Section, Spinner} from "@radix-ui/themes";
import Loader from "@/components/Loader";

const Loading = () => {
    return (
         <Loader/>
    );
};

export default Loading;
